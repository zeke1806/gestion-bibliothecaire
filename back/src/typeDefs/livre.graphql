type Livre {
  # numLivre
  id: ID!
  design: String!
  auteur: String!
  dateEdition: String!
  disponible: Boolean!
  nbPret: Int
}

input LivreInput {
  design: String!
  auteur: String!
  dateEdition: String!
  disponible: Boolean!
}
